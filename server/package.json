{
  "name": "waldo",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd .. && cd client && npm run build && rm -rf ../server/dist && mkdir -p ../server/dist && mv dist ../server/dist",
    "deploy": "npm run build && npx prisma generate && npx prisma migrate deploy && node app"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "dependencies": {
    "@prisma/client": "^6.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.1",
    "prisma": "^6.0.1"
  }
}
